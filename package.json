{
  "name": "edusmart-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "EduAssess AI — Smart Question Ingestion, Mobile-First Assessment & AI-Assisted Evaluation Platform",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run --workspaces --if-present build",
    "test": "node tools/run-all-tests.mjs",
    "lint": "npm run --workspaces --if-present lint"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "bullmq": "^6.3.8"
  }
}
